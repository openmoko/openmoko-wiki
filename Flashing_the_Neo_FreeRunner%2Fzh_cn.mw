    OpenMoko会不断发布最新的根文件系统（root file system)、内核（kernel）和Bootloader（U-boot）的二进制映像（binary image）文件。这些文件可以被烧写到Neo FreeRunner的NAND Flash中，从而达到更新系统套件的目的。你可以通过一根USB线，连接FreeRunner到电脑上，通过OpenMoko提供的烧写工具来更新系统。
