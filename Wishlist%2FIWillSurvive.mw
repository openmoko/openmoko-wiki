{{Wishlist}}
=== I Will Survice ===
A Gps monitor for crash report


=== The Main Idea ===
The idea is to do a programm that receive data from gps and check if the speed decrease too fast. In this case a crash maybe will happened. 
The application will ask to human if it's ok and if human does not respond (for example by press the screen) a emergency call to a previews set number communicating the gps position.
